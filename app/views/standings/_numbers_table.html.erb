<div class= "table-responsive">
  <table class="table table-hover">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Name</th>
        <% @rounds.each do |round| %>
          <th scope="col" class="text-end" colspan="2"><%= @round_names[round] %></th>
          <td></td>
          <td></td>
        <% end %>
        <th scope="col" class="text-end" colspan="2">Total</th>
      </tr>
      <tr>
        <td></td>
        <td></td>
        <% @rounds.each do |round| %>
          <td class="text-end">Min</td>
          <td class="text-end">Max</td>
          <td></td>
          <td></td>
        <% end %>
        <td class="text-end">Min</td>
        <td class="text-end">Max</td>
      </tr>
    </thead>
    <tbody>
      <%= render partial: 'standings/user_numbers_row', collection: @data, as: :user_data %>
    </tbody>
  </table>
</div>
